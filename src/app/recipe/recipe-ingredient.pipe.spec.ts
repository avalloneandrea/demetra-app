import { RecipeIngredientPipe } from './recipe-ingredient.pipe';

describe('RecipeIngredientPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeIngredientPipe();
    expect(pipe).toBeTruthy();
  });
});
